# Contribuir

Ejecuta `python3 -m unittest discover -s tests -v` y
`bash tests/native-smoke.sh`. No incluyas bases Odoo, tokens, credenciales ni
datos de clientes. Las contribuciones se publican bajo AGPL-3.0.
